import { readJsonFile } from './_lib/github.js';
const TYPES=['all','connections','crosswords','pyramids'];
function formatSeconds(seconds){const value=Math.max(0,Math.round(Number(seconds)||0));const hours=Math.floor(value/3600);const minutes=Math.floor((value%3600)/60);const secs=value%60;return hours?`${hours}h ${minutes}m`:`${minutes}m ${secs}s`;}
export default async function handler(req,res){if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'Method not allowed'});}const category=String(req.query?.category||'all');if(!TYPES.includes(category))return res.status(400).json({error:'Invalid category.'});try{const current=await readJsonFile('data/results.json',{results:[]});const results=Array.isArray(current.data.results)?current.data.results:[];const filtered=category==='all'?results:results.filter(result=>result.type===category);const byUser=new Map();for(const result of filtered){const row=byUser.get(result.username)||{username:result.username,points:0,solved:0,totalTimeSeconds:0};row.points+=Number(result.score||0);row.solved+=result.completed?1:0;row.totalTimeSeconds+=Math.max(0,Number(result.solveTimeSeconds)||0);byUser.set(result.username,row);}const rankings=[...byUser.values()].sort((a,b)=>b.points-a.points||b.solved-a.solved||a.totalTimeSeconds-b.totalTimeSeconds||a.username.localeCompare(b.username)).map((row,index)=>{const denominator=Math.max(1,row.solved);return{rank:index+1,...row,averageTime:row.solved?formatSeconds(row.totalTimeSeconds/denominator):'—',totalTime:formatSeconds(row.totalTimeSeconds)};});return res.status(200).json({category,rankings});}catch(error){console.error(error);return res.status(500).json({error:'Could not load rankings.'});}}

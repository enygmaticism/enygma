const loginPanel=document.getElementById('login-panel'),editorPanel=document.getElementById('editor-panel'),loginForm=document.getElementById('login-form'),entryForm=document.getElementById('entry-form'),loginMessage=document.getElementById('login-message'),entryMessage=document.getElementById('entry-message'),manageMessage=document.getElementById('manage-message'),editMessage=document.getElementById('edit-message'),typeSelect=document.getElementById('entry-type'),standardFields=document.getElementById('standard-content-fields'),connectionsFields=document.getElementById('connections-fields'),connectionsGroups=document.getElementById('connections-groups'),pyramidsFields=document.getElementById('pyramids-fields'),manageType=document.getElementById('manage-type'),puzzleList=document.getElementById('puzzle-list'),editPanel=document.getElementById('edit-panel'),editForm=document.getElementById('edit-form'),editConnectionsFields=document.getElementById('edit-connections-fields'),editPyramidsFields=document.getElementById('edit-pyramids-fields'),editConnectionsGroups=document.getElementById('edit-connections-groups');
const DIFFICULTIES=[{color:'yellow',label:'Yellow — easiest'},{color:'green',label:'Green'},{color:'blue',label:'Blue'},{color:'purple',label:'Purple — hardest'}];
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function letters(v){return String(v||'').toUpperCase().replace(/[^A-ZČĆŽŠĐ]/g,'');}
function renderConnectionGroups(container=connectionsGroups,prefix='new'){container.innerHTML=DIFFICULTIES.map((d,i)=>`<fieldset class="connections-group-editor ${d.color}"><legend>${d.label}</legend><label>Category name<input class="connection-category" data-prefix="${prefix}" data-group="${i}" required></label><div class="connection-word-grid">${[0,1,2,3].map(w=>`<label>Word ${w+1}<input class="connection-word" data-prefix="${prefix}" data-group="${i}" data-word="${w}" required></label>`).join('')}</div></fieldset>`).join('');}
function setConnectionGroups(container,groups,prefix){renderConnectionGroups(container,prefix);(groups||[]).forEach((g,i)=>{const n=container.querySelector(`.connection-category[data-prefix="${prefix}"][data-group="${i}"]`);if(n)n.value=g.name||'';(g.words||[]).forEach((w,j)=>{const x=container.querySelector(`.connection-word[data-prefix="${prefix}"][data-group="${i}"][data-word="${j}"]`);if(x)x.value=w||'';});});}
function readConnectionGroups(container,prefix){return DIFFICULTIES.map((d,i)=>({name:container.querySelector(`.connection-category[data-prefix="${prefix}"][data-group="${i}"]`).value.trim(),color:d.color,words:[...container.querySelectorAll(`.connection-word[data-prefix="${prefix}"][data-group="${i}"]`)].map(x=>x.value.trim())}));}
function renderBranchRows(container,count,rows=[]){const n=Math.max(0,Math.min(29,Number(count)-1));container.innerHTML=Array.from({length:n},(_,i)=>{const row=rows[i]||{};return `<div class="pyramid-row-editor"><h3>Row ${i+2} — ${i+2} letters</h3><div class="pyramid-row-editor-grid"><label>Clue<input class="branch-clue" data-row="${i}" required value="${esc(row.clue||'')}"></label><label>Answer<input class="branch-answer" data-row="${i}" required value="${esc(row.answer||'')}"></label></div></div>`;}).join('');}
function readBranchRows(container){return[...container.querySelectorAll('.pyramid-row-editor')].map((_,i)=>({clue:container.querySelector(`.branch-clue[data-row="${i}"]`).value.trim(),answer:letters(container.querySelector(`.branch-answer[data-row="${i}"]`).value)}));}
function updatePyramidRows(){const n=document.getElementById('pyramid-row-count').value;renderBranchRows(document.getElementById('pyramid-branch-a'),n);renderBranchRows(document.getElementById('pyramid-branch-b'),n);}
function updateEditPyramidRows(a=[],b=[]){const n=document.getElementById('edit-pyramid-row-count').value;renderBranchRows(document.getElementById('edit-pyramid-branch-a'),n,a);renderBranchRows(document.getElementById('edit-pyramid-branch-b'),n,b);}
function updateEditorMode(){const t=typeSelect.value;standardFields.classList.toggle('hidden',t!=='crosswords');connectionsFields.classList.toggle('hidden',t!=='connections');pyramidsFields.classList.toggle('hidden',t!=='pyramids');}
function showEditor(){loginPanel.classList.add('hidden');editorPanel.classList.remove('hidden');loadPuzzleList();}
function showLogin(msg=''){loginPanel.classList.remove('hidden');editorPanel.classList.add('hidden');editPanel.classList.add('hidden');loginMessage.textContent=msg;}
async function checkAdminAccess(){try{const r=await fetch('api/admin/check',{cache:'no-store'});if(r.ok)showEditor();else showLogin('Enter your admin password.');}catch{showLogin('Could not reach the admin service.');}}
loginForm.addEventListener('submit',async e=>{e.preventDefault();loginMessage.textContent='Checking…';try{const r=await fetch('api/admin/check',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({password:document.getElementById('password').value})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Incorrect password.');document.getElementById('password').value='';showEditor();}catch(err){loginMessage.textContent=err.message;}});
entryForm.addEventListener('submit',async e=>{e.preventDefault();entryMessage.textContent='Saving…';const type=typeSelect.value;let payload={type,title:document.getElementById('entry-title').value.trim(),date:document.getElementById('entry-date').value,content:document.getElementById('entry-content').value},endpoint='api/admin/entries';if(type==='connections'){payload.groups=readConnectionGroups(connectionsGroups,'new');payload.content='';}if(type==='pyramids'){payload={title:payload.title,date:payload.date,rowCount:Number(document.getElementById('pyramid-row-count').value),firstClue:document.getElementById('pyramid-first-clue').value.trim(),firstAnswers:[letters(document.getElementById('pyramid-first-a').value),letters(document.getElementById('pyramid-first-b').value)],branches:[{rows:readBranchRows(document.getElementById('pyramid-branch-a'))},{rows:readBranchRows(document.getElementById('pyramid-branch-b'))}]};endpoint='api/admin/pyramids';}try{const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify(payload)}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not save entry.');entryMessage.textContent='Entry added to GitHub successfully.';entryForm.reset();document.getElementById('entry-date').value=new Date().toISOString().slice(0,10);renderConnectionGroups();updatePyramidRows();updateEditorMode();loadPuzzleList();}catch(err){entryMessage.textContent=err.message;}});
typeSelect.addEventListener('change',updateEditorMode);document.getElementById('pyramid-row-count').addEventListener('input',updatePyramidRows);manageType.addEventListener('change',loadPuzzleList);document.getElementById('refresh-puzzles').addEventListener('click',loadPuzzleList);document.getElementById('cancel-edit').addEventListener('click',()=>editPanel.classList.add('hidden'));document.getElementById('logout-button').addEventListener('click',async()=>{await fetch('api/admin/check',{method:'DELETE',credentials:'same-origin'});showLogin('You have been logged out.');});
async function loadPuzzleList(){manageMessage.textContent='Loading…';try{const type=manageType.value,endpoint=type==='pyramids'?'api/admin/pyramids':`api/admin/puzzles?type=${encodeURIComponent(type)}`,r=await fetch(endpoint,{cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not load puzzles.');const entries=[...(d.entries||[])].sort((a,b)=>new Date(b.date)-new Date(a.date)||String(b.id).localeCompare(String(a.id)));puzzleList.innerHTML=entries.length?entries.map(e=>`<div class="puzzle-manager-item"><div><strong>${esc(e.title||e.date)}</strong><span>${esc(e.date)}</span></div><div class="puzzle-manager-actions"><button type="button" class="secondary edit-puzzle" data-id="${esc(e.id)}">Edit</button><button type="button" class="danger delete-puzzle" data-id="${esc(e.id)}">Delete</button></div></div>`).join(''):'<div class="empty">No puzzles in this category.</div>';puzzleList.querySelectorAll('.edit-puzzle').forEach(b=>b.addEventListener('click',()=>editPuzzle(entries.find(x=>String(x.id)===b.dataset.id),type)));puzzleList.querySelectorAll('.delete-puzzle').forEach(b=>b.addEventListener('click',()=>deletePuzzle(b.dataset.id,type)));manageMessage.textContent=entries.length?`${entries.length} puzzle${entries.length===1?'':'s'}.`:'';}catch(err){puzzleList.innerHTML='';manageMessage.textContent=err.message;}}
function editPuzzle(entry,type){editPanel.classList.remove('hidden');document.getElementById('edit-heading').textContent=`Edit ${entry.title||entry.date}`;document.getElementById('edit-id').value=entry.id;document.getElementById('edit-title').value=entry.title||'';document.getElementById('edit-date').value=entry.date||'';document.getElementById('edit-content').value=entry.content||'';editForm.dataset.type=type;editConnectionsFields.classList.toggle('hidden',type!=='connections');editPyramidsFields.classList.toggle('hidden',type!=='pyramids');document.getElementById('edit-standard-fields').classList.toggle('hidden',type!=='crosswords');if(type==='connections')setConnectionGroups(editConnectionsGroups,entry.groups,'edit');if(type==='pyramids'){document.getElementById('edit-pyramid-row-count').value=entry.rowCount||1;document.getElementById('edit-pyramid-first-clue').value=entry.firstClue||'';document.getElementById('edit-pyramid-first-a').value=entry.firstAnswers?.[0]||'';document.getElementById('edit-pyramid-first-b').value=entry.firstAnswers?.[1]||'';updateEditPyramidRows(entry.branches?.[entry.firstAnswers?.[0]]?.rows||[],entry.branches?.[entry.firstAnswers?.[1]]?.rows||[]);}editPanel.scrollIntoView({behavior:'smooth'});}
editForm.addEventListener('submit',async e=>{e.preventDefault();editMessage.textContent='Saving changes…';const type=editForm.dataset.type,id=document.getElementById('edit-id').value;let endpoint='api/admin/puzzles',body={type,id,entry:{title:document.getElementById('edit-title').value.trim(),date:document.getElementById('edit-date').value,content:document.getElementById('edit-content').value}};if(type==='connections')body.entry.groups=readConnectionGroups(editConnectionsGroups,'edit');if(type==='pyramids'){endpoint='api/admin/pyramids';body={id,title:document.getElementById('edit-title').value.trim(),date:document.getElementById('edit-date').value,rowCount:Number(document.getElementById('edit-pyramid-row-count').value),firstClue:document.getElementById('edit-pyramid-first-clue').value.trim(),firstAnswers:[letters(document.getElementById('edit-pyramid-first-a').value),letters(document.getElementById('edit-pyramid-first-b').value)],branches:[{rows:readBranchRows(document.getElementById('edit-pyramid-branch-a'))},{rows:readBranchRows(document.getElementById('edit-pyramid-branch-b'))}]};}try{const r=await fetch(endpoint,{method:'PUT',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify(body)}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not save changes.');editMessage.textContent='Changes saved to GitHub.';await loadPuzzleList();}catch(err){editMessage.textContent=err.message;}});
async function deletePuzzle(id,type){if(!confirm('Delete this puzzle? This cannot be undone.'))return;manageMessage.textContent='Deleting…';try{const endpoint=type==='pyramids'?'api/admin/pyramids':'api/admin/puzzles',r=await fetch(endpoint,{method:'DELETE',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({type,id})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not delete puzzle.');editPanel.classList.add('hidden');await loadPuzzleList();}catch(err){manageMessage.textContent=err.message;}}
document.getElementById('entry-date').value=new Date().toISOString().slice(0,10);renderConnectionGroups();updatePyramidRows();updateEditorMode();checkAdminAccess();

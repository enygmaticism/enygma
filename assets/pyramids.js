let puzzle=null;let locked=false;
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function units(value){const clean=String(value||'').toUpperCase().replace(/[^A-ZČĆŽŠĐ]/g,'');const out=[];for(let i=0;i<clean.length;i+=1){const pair=clean.slice(i,i+2);if(['DŽ','LJ','NJ'].includes(pair)){out.push(pair);i+=1;}else out.push(clean[i]);}return out;}
function rowsFromPuzzle(){return [{index:0,clue:puzzle.row?.clue||'',length:1},...(puzzle.branchRows||[]).map(r=>({index:r.index,clue:r.clue,length:r.length}))];}
function attempts(index){return Number((puzzle.guesses||{})[index]||0);}
function failed(index){return (puzzle.failedRows||[]).includes(index);}
function renderRow(row){const solved=puzzle.revealedRows?.[row.index]?.answer;const exhausted=failed(row.index);const left=Math.max(0,3-attempts(row.index));const active=!solved&&!exhausted;const cells=solved?units(solved).map(u=>`<div class="pyramid-cell solved-cell">${esc(u)}</div>`).join(''):exhausted?Array.from({length:row.length},()=>'<div class="pyramid-cell failed-cell">×</div>').join(''):Array.from({length:row.length},(_,i)=>`<div class="pyramid-cell">${active?`<input class="pyramid-current-input" maxlength="2" inputmode="text" autocomplete="off" aria-label="Row ${row.index+1}, letter ${i+1}" data-row-index="${row.index}" data-col="${i}">`:''}</div>`).join('');let status=solved?'Solved':exhausted?'No tries left':`${left} ${left===1?'try':'tries'} left`;return `<div class="pyramid-row-block ${active?'active-pyramid-row':''} ${solved?'row-solved':''} ${exhausted?'row-failed':''}"><div class="pyramid-row">${cells}</div><div class="pyramid-clue"><span class="pyramid-row-number">${row.index+1}</span><span class="pyramid-clue-text">${esc(row.clue)}</span><span class="pyramid-tries">${status}</span></div>${active?`<button type="button" class="pyramid-row-check" data-row-check="${row.index}">Check answer</button>`:''}</div>`;}
function bindInputs(){document.querySelectorAll('.pyramid-current-input').forEach(input=>input.addEventListener('input',e=>{e.target.value=e.target.value.toUpperCase().replace(/[^A-ZČĆŽŠĐ]/g,'').slice(0,2);const value=e.target.value;if(value.length===2&&!['DŽ','LJ','NJ'].includes(value))e.target.value=value.slice(-1);const row=e.target.dataset.rowIndex;const all=[...document.querySelectorAll(`.pyramid-current-input[data-row-index="${row}"]`)];const idx=all.indexOf(e.target);if(value&&idx<all.length-1)all[idx+1].focus();}));document.querySelectorAll('.pyramid-row-check').forEach(btn=>btn.addEventListener('click',()=>submitRow(Number(btn.dataset.rowCheck))));}
function render(){const app=document.getElementById('pyramid-app');if(!puzzle){app.innerHTML='<div class="connections-empty">no entries currently.</div>';return;}if(puzzle.alreadyPlayed){app.innerHTML='<div class="pyramid-result"><h2>Already played</h2><p>This is not your first time playing this puzzle, so it will not influence the ranking.</p><p>Your original result is already recorded.</p></div>';return;}const unlocked=Boolean(puzzle.openingUnlocked);const rows=unlocked?rowsFromPuzzle():[{index:0,clue:puzzle.row?.clue||'',length:1}];app.innerHTML=`<div class="pyramid-game ${unlocked?'pyramid-unlocked':''}"><div class="connections-date">${esc(puzzle.date)}</div><div class="pyramid-board-wrap"><div class="pyramid-board">${rows.map(renderRow).join('')}</div></div><div id="pyramid-message" class="pyramid-message"></div><div class="pyramid-login-note">You have three tries for each row.</div></div>`;bindInputs();}
async function submitRow(rowIndex){if(locked)return;const inputs=[...document.querySelectorAll(`.pyramid-current-input[data-row-index="${rowIndex}"]`)];const row=rowsFromPuzzle().find(r=>r.index===rowIndex)||{length:1};const answer=inputs.map(i=>i.value).join('');const message=document.getElementById('pyramid-message');if(units(answer).length!==row.length){message.textContent=`Enter exactly ${row.length} Serbian letter${row.length===1?'':'s'}.`;return;}locked=true;message.textContent='Checking…';try{const r=await fetch('api/pyramids',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({puzzleId:puzzle.id,rowIndex,answer})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not submit the answer.');if(d.incorrect){puzzle.guesses=d.guesses||puzzle.guesses||{};puzzle.failedRows=d.failedRows||puzzle.failedRows||[];locked=false;message.textContent=d.failed?'No tries left for this row.':'incorrect';render();return;}if(d.completed){puzzle.revealedRows=(d.revealedAnswers||[]).map((a,index)=>a?{answer:a,index}:null).filter(Boolean);document.getElementById('pyramid-app').innerHTML=`<div class="pyramid-result"><h2>Solved!</h2><p>${Number(d.score).toLocaleString()} points · ${formatTime(d.solveTimeSeconds)}</p><p>Your result has been recorded.</p></div>`;return;}puzzle.openingUnlocked=true;puzzle.branchRows=d.branchRows||puzzle.branchRows||[];puzzle.revealedRows=(d.revealedAnswers||[]).map((a,index)=>a?{answer:a,index}:null).filter(Boolean);puzzle.guesses=d.guesses||puzzle.guesses||{};puzzle.failedRows=d.failedRows||puzzle.failedRows||[];locked=false;message.textContent='Correct.';render();}catch(err){locked=false;message.textContent=err.message;}}
function formatTime(s){const v=Number(s||0),m=Math.floor(v/60),sec=v%60;return `${m}:${String(sec).padStart(2,'0')}`;}
async function init(){try{const id=new URLSearchParams(location.search).get('id'),url=id?`api/pyramids?id=${encodeURIComponent(id)}`:'api/pyramids';const r=await fetch(`${url}${url.includes('?')?'&':'?'}t=${Date.now()}`,{credentials:'same-origin',cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not load Pyramid.');puzzle=d.puzzle;render();const archive=document.getElementById('pyramid-archive-list'),entries=d.archive||[];archive.innerHTML=entries.length?entries.map((e,i)=>`<a class="pyramid-archive-item" href="pyramids.html?id=${encodeURIComponent(e.id)}"><span>${esc(e.title)}</span><span>${esc(e.date)}${i===0?' · latest':''}</span></a>`).join(''):'<div class="connections-empty">no entries currently.</div>`;}catch(err){document.getElementById('pyramid-app').innerHTML=`<div class="connections-empty">${esc(err.message)}</div>`;}}
init();

const tabs=document.querySelectorAll('.ranking-tab');const list=document.getElementById('ranking-list');
async function load(category){list.innerHTML='<div class="empty">Loading rankings…</div>';try{const response=await fetch(`api/rankings?category=${encodeURIComponent(category)}`,{cache:'no-store'});const result=await response.json();if(!response.ok)throw new Error(result.error||'Could not load rankings.');list.innerHTML=result.rankings.length?result.rankings.map(row=>`<div class="ranking-row"><span class="rank-number">${row.rank}</span><span class="rank-user">${row.username}</span><span>${row.solved}</span><strong>${Number(row.points).toLocaleString()}</strong><span>${row.averageTime||'—'}</span><span>${row.totalTime||'0m 0s'}</span></div>`).join(''):'<div class="empty">No results yet.</div>';}catch(error){list.innerHTML=`<div class="empty">${error.message}</div>`;}}
tabs.forEach(tab=>tab.addEventListener('click',()=>{tabs.forEach(item=>item.classList.toggle('active',item===tab));load(tab.dataset.category);}));load('all');
